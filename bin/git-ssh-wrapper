#!/bin/sh
DIRSCRIPT=$(dirname "$(readlink -f "$0")")

exec ssh -oCheckHostIP=no -oControlMaster=auto -oControlPath=~/.ssh/l10n-bot-%C -oControlPersist=5 -oHashKnownHosts=no -oUserKnownHostsFile="$DIRSCRIPT/data/known_hosts" "$@"
