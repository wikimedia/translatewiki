<?php

$DOMAIN = '<%= @domain %>';
$CONF = '<%= @config_dir %>';
$IP = __DIR__;

$wgSecretKey = file_get_contents( "$IP/../secretkey" );
require_once "$CONF/mw-config/<%= @mediawiki_config %>";
require_once "$IP/../CustomSettings.php";
