# file managed by puppet

description "MediaWiki jobrunner for production"

start on mysql
stop on shutdown

setuid betawiki
setgid betawiki

exec php /srv/mediawiki/targets/production/maintenance/runJobs.php --wait --maxjobs=1000 \
>> /www/translatewiki.net/logs/jobqueue
respawn
