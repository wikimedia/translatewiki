# This file is managed by puppet
[Unit]
Description=Run autobackport-mediawiki for a given branch on a defined schedule

[Timer]
OnCalendar=<%= @when %>
Unit=autobackport-mediawiki@<%= @branch %>.service

[Install]
WantedBy=timers.target
