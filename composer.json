{
	"require-dev": {
		"jakub-onderka/php-parallel-lint": "1.0.0",
		"mediawiki/mediawiki-codesniffer": "28.0.0"
	},
	"scripts": {
		"fix": "phpcbf",
		"test": [
			"parallel-lint . bin/lint-bash --exclude vendor --exclude repong/vendor",
			"bin/lint-bash",
			"phpcs -p -s"
		]
	}
}
